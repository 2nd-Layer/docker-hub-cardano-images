on: push
name: Test Docker Build for images

jobs:
  docker-build:
    name: Build Docker images
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v2
      - name: Build Docker images
        run: .github/tests/00_docker-build.sh
      